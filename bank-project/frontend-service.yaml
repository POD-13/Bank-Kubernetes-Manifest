apiVersion: v1
kind: Service
metadata:
  name: bank-frontend-service
  namespace: banking
spec:
  selector:
    app: bank-frontend-deployment
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP